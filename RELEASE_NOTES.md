# Release v0.4.5

## 新功能

### 1. 统一通知系统
- 新增 `NotificationDispatcher` 统一通知分发器，集中管理三条通知渠道
- **首页通知栏** (`homepage-banner`): 显示系统重要通知
- **最近维护面板** (`maintenance-timeline`): 显示任务进度和完成状态
- **SMTP 邮件** (`smtp`): 发送邮件通知（需配置）

### 2. 任务进度通知
- 所有分析任务现在都会向"最近维护"面板推送进度通知
- 订阅源添加时的视频基本信息分析也会显示进度
- 任务失败时同时推送到首页通知栏和邮件

### 3. 配置备份与恢复
- 支持导出所有配置为 JSON 文件
- 支持从 JSON 文件导入恢复配置
- 导出内容包括：系统配置、SMTP配置、通知设置、订阅源列表

### 4. 日志系统优化
- 日志默认开启记录
- Celery Worker 日志现在也会写入数据库
- 新增 Celery Beat 定时清理任务（每小时执行）
- 日志保留时间可通过设置面板动态调整，无需重启

### 5. M3U 播放列表格式修复
- 修复播放列表 Content-Type 为标准 `audio/x-mpegurl`
- 播放列表通过 Nginx 代理，统一使用前端端口访问
- 播放器现在可以正确识别和加载播放列表

### 6. UI/UX 优化
- 首页播放列表改为卡片式 Grid 布局
- 设置面板卡片统一宽度，两列等宽布局
- 完善手机端响应式适配
- 桌面端内容区域限制最大宽度 1200px
- 网页标题动态更新（如 `iptv-manager｜首页`）

## Bug 修复

- 修复 API 中 `analyze_video=False` 硬编码问题
- 修复 Celery Worker 数据库连接池问题（使用 `NullPool`）
- 修复 Dockerfile 国内镜像源加速问题

## 架构改进

- 数据库连接池使用 `NullPool`，解决 Celery fork 后连接共享问题
- Dockerfile 添加阿里云镜像源加速
- 新增 `log_cleanup_scheduler` 定时任务

## 致谢

本版本由 **Trae IDE** 中的 **GLM-5** 模型协助开发和重构完成。

特别感谢用户的高质量提示词指导：

1. **通知系统重构**: 用户发现原 Claude Opus 4.6 编写的分析任务队列缺少通知模块集成，提出统一三条通知渠道的需求
2. **日志功能完善**: 用户指出设置面板日志功能不可用，并提出默认开启、定时清理的需求
3. **配置备份功能**: 用户发现备份/恢复按钮是摆设，要求实现完整功能
4. **播放列表格式**: 用户发现 M3U 格式不被播放器识别，需要修复
5. **UI 优化**: 用户参照 Element Plus 设计规范，要求优化手机适配和面板宽度
6. **端口映射说明**: 用户补充了后端 8000 端口映射，用于直接访问 API

这些清晰的反馈和需求描述极大地推动了 v0.4.0 版本的顺利完成。

## 升级说明

1. 拉取最新镜像：`docker-compose pull`
2. 重启服务：`docker-compose up -d`
3. 访问 `http://<your-server-ip>:8001` 使用前端界面
4. 后端 API 可通过 `http://<your-server-ip>:8000` 直接访问
